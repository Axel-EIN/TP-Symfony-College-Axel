{% extends 'base.html.twig' %}

{% block body %}

<div>
    <h1 class="my-5">Bienvenue sur {{ titre }} !</h1>

    {% for une_note in notes %}
        <div class="card">
            <div class="card-body">
                <h4 class="card-title"><strong class="display-4">{{ une_note.note }}</strong><small> /20</small> en <strong>{{ une_note.matiere.nom }}</strong></h4>
                <p>de <strong>{{ une_note.eleve.nom }} {{ une_note.eleve.prenom }}</strong> attribuée le {{ une_note.date.format('d/m/Y') }}</p>
            </div>
        </div>
    {% endfor %}
</div>
{% endblock %}
